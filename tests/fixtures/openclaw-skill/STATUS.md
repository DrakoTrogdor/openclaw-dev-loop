# Status

## Known Issues

- `save` accepts any string as a URL — no validation
- `export --format csv` is documented but not implemented
- Tag search is case-sensitive but tags are stored lowercase (mismatch)
- Date range filtering not yet implemented
- `migrate` command mentioned in STORAGE.md but doesn't exist in the script
